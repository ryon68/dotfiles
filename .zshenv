#=====================
# zsh env settings
#=====================
#{{{

# zmodload zsh/zprof && zprof

export ZDOTDIR="${HOME}/.zsh"
# export ZDOTDIR="${HOME}/dotfiles/.zsh"

# for src in ${ZDOTDIR}/zshrc/*.zsh ${ZDOTDIR}/zshenv/*.zsh; do
#   [ ${ZDOTDIR}/.zcompdump -nt $src ] && continue
#   source ${ZDOTDIR}/make.sh
# done
# }}}
